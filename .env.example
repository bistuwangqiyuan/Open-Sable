# ============================================
# OPEN-SABLE CONFIGURATION FILE
# Copy this to .env and fill in your values
# ============================================

# ============================================
# INTERFACE MODE
# ============================================
# CLI Mode - Terminal chat interface (no external messenger needed)
CLI_ENABLED=false

# ============================================
# CORE LLM SETTINGS
# ============================================
OLLAMA_BASE_URL=http://localhost:11434
DEFAULT_MODEL=llama3.1:8b
AUTO_SELECT_MODEL=true

# API Keys for external LLM providers (optional)
# You only need ONE provider. The agent tries them in order until one works.
# Priority: Ollama (local) → OpenAI → DeepSeek → Groq → Together → xAI →
#           Mistral → Kimi → Qwen → OpenRouter → Open WebUI → Anthropic → Gemini → Cohere

# --- OpenAI-compatible providers ---
OPENAI_API_KEY=
DEEPSEEK_API_KEY=
GROQ_API_KEY=
TOGETHER_API_KEY=
XAI_API_KEY=
MISTRAL_API_KEY=
KIMI_API_KEY=
# (alias: MOONSHOT_API_KEY)
QWEN_API_KEY=
# (alias: DASHSCOPE_API_KEY)
OPENROUTER_API_KEY=

# --- Open WebUI (self-hosted, OpenAI-compatible) ---
# Connect to your own Open WebUI / Ollama instance with a public URL
OPENWEBUI_API_KEY=
OPENWEBUI_API_URL=
# e.g. https://your-server.com/api
OPENWEBUI_MODEL=
# e.g. llama3.2:latest, mistral:latest, etc.

# --- Native SDK providers ---
ANTHROPIC_API_KEY=
GEMINI_API_KEY=
COHERE_API_KEY=

# ============================================
# TELEGRAM BOT (Primary Interface)
# ============================================
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
TELEGRAM_ALLOWED_USERS=user_id_1,user_id_2

# Telegram Userbot (Advanced - for monitoring groups/channels)
TELEGRAM_USERBOT_ENABLED=false
TELEGRAM_API_ID=
TELEGRAM_API_HASH=
TELEGRAM_PHONE_NUMBER=
TELEGRAM_SESSION_NAME=opensable_session
USERBOT_AUTO_RESPOND=true

# ============================================
# OTHER CHAT PLATFORMS
# ============================================
DISCORD_BOT_TOKEN=
DISCORD_GUILD_ID=
WHATSAPP_ENABLED=false

# IRC Configuration
IRC_SERVER=irc.libera.chat
IRC_PORT=6667
IRC_NICKNAME=opensable_bot
IRC_CHANNELS=#opensable,#ai

# Matrix Configuration
MATRIX_HOMESERVER=https://matrix.org
MATRIX_USER=@opensable:matrix.org
MATRIX_PASSWORD=

# Slack Configuration
SLACK_BOT_TOKEN=
SLACK_APP_TOKEN=

# ============================================
# EMAIL & CALENDAR INTEGRATION
# ============================================
GMAIL_ENABLED=true
GMAIL_CREDENTIALS_PATH=./config/gmail_credentials.json
CALENDAR_ENABLED=true
CALENDAR_CREDENTIALS_PATH=./config/calendar_credentials.json

# ============================================
# SECURITY & SANDBOX
# ============================================
ENABLE_SANDBOX=true
ALLOWED_DOMAINS=*.google.com,*.booking.com,*.airbnb.com
MAX_FILE_SIZE_MB=100

# ============================================
# AGENT BEHAVIOR
# ============================================
AGENT_NAME=Sable
AGENT_PERSONALITY=helpful
# Options: helpful, professional, sarcastic, meme-aware
HEARTBEAT_INTERVAL=300
MAX_RETRIES=3

# ============================================
# AUTONOMOUS MODE (Background Task Execution)
# ============================================
AUTONOMOUS_ENABLED=false
AUTONOMOUS_CHECK_INTERVAL=60
AUTONOMOUS_MAX_TASKS=3
AUTONOMOUS_SOURCES=calendar,email,scheduled_tasks

# ============================================
# MEMORY & STORAGE
# ============================================
MEMORY_RETENTION_DAYS=90
VECTOR_DB_PATH=./data/vectordb
DATA_DIR=./data

# ============================================
# LOGGING
# ============================================
LOG_LEVEL=INFO
LOG_FILE=./logs/opensable.log

# ============================================
# VOICE INTERFACE (🧪 Experimental)
# ============================================
VOICE_ENABLED=false
VOICE_STT_MODEL=base
# Options: tiny, base, small, medium, large
VOICE_TTS_VOICE=en-us-blizzard_lessac-medium
VOICE_INPUT_DEVICE=default
VOICE_OUTPUT_DEVICE=default
VOICE_DEVICE=cpu

# ============================================
# MULTIMODAL (Vision & Audio Processing)
# ============================================
VISION_ENABLED=false
VISION_MODEL=yolov8n
VISION_CACHE_SIZE=1000
VISION_DEVICE=cpu
VISION_CACHE_DIR=./models/vision
AUDIO_CACHE_SIZE=500
AUDIO_CACHE_DIR=./models/audio
OCR_ENABLED=false

# ============================================
# AGI CORE SUBSYSTEMS
# ============================================
AGI_STORAGE_DIR=./data/agi
AGI_MEMORY_CONSOLIDATION_INTERVAL=1
AGI_META_LEARNING_INTERVAL=24
AGI_MAX_EPISODIC_MEMORIES=10000
AGI_MAX_SEMANTIC_MEMORIES=50000
AGI_WORKING_MEMORY_CAPACITY=7
AGI_TOOL_SYNTHESIS_ENABLED=true
AGI_TOOL_SAFETY_STRICT=true

# ============================================
# MULTI-DEVICE SYNC (🗺️ Planned)
# ============================================
SYNC_ENABLED=false
SYNC_SERVER_URL=ws://localhost:8080
SYNC_ENCRYPTION_KEY=
SYNC_ENABLE_ENCRYPTION=false
SYNC_CONFLICT_STRATEGY=latest_wins
# Options: latest_wins, manual, server_wins, client_wins
SYNC_INTERVAL=30

# ============================================
# GOAL SYSTEM & META-LEARNING
# ============================================
GOAL_STORAGE_PATH=./data/goals
GOAL_MAX_DEPTH=5
GOAL_DECOMPOSE_ENABLED=true
META_LEARNING_ENABLED=false

# ============================================
# TOOL SYNTHESIS (Dynamic Tool Creation)
# ============================================
TOOL_SYNTHESIS_ENABLED=false
TOOL_STORAGE_PATH=./data/synthesized_tools
TOOL_MAX_COMPLEXITY=medium

# ============================================
# SKILLS MARKETPLACE (🗺️ Planned)
# ============================================
SKILLS_REGISTRY_URL=
SKILLS_API_URL=
SKILLS_AUTO_UPDATE=true
SKILLS_VERIFY_SIGNATURES=true
SKILLS_DIR=./skills

# ============================================
# DATABASE CONNECTIONS (Optional)
# ============================================
DATABASE_TYPE=sqlite
# Options: sqlite, postgres, mysql, mongodb, redis
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=opensable
POSTGRES_USER=
POSTGRES_PASSWORD=
MONGODB_URI=
REDIS_HOST=localhost
REDIS_PORT=6379

# ============================================
# MONITORING & OBSERVABILITY
# ============================================
PROMETHEUS_ENABLED=false
PROMETHEUS_PORT=9090
METRICS_EXPORT_INTERVAL=60

# ============================================
# ENTERPRISE FEATURES
# ============================================
MULTI_TENANT_ENABLED=false
RBAC_ENABLED=false
AUDIT_LOG_ENABLED=false
WEBHOOK_ENABLED=false
WEBHOOK_URL=

# ============================================
# BROWSER AUTOMATION
# ============================================
BROWSER_HEADLESS=true
BROWSER_TIMEOUT=30
BROWSER_MAX_TABS=5

# ============================================
# RATE LIMITING
# ============================================
RATE_LIMIT_ENABLED=true
RATE_LIMIT_REQUESTS=60
RATE_LIMIT_WINDOW=60

# ============================================
# MOBILE API
# ============================================
MOBILE_API_ENABLED=false
MOBILE_API_PORT=8000
MOBILE_JWT_SECRET=
MOBILE_ALLOWED_ORIGINS=http://localhost:3000

# ============================================
# ANALYTICS & TELEMETRY
# ============================================
ANALYTICS_ENABLED=false
TELEMETRY_ENABLED=false
USAGE_STATS_ENABLED=false

# ============================================
# ADVANCED
# ============================================
DEBUG_MODE=false
ENABLE_EXPERIMENTAL_FEATURES=false
MAX_CONTEXT_LENGTH=8192
CACHE_TTL=3600
WORKER_THREADS=4
MAX_CONCURRENT_REQUESTS=100

# ============================================
# PLUGIN SYSTEM
# ============================================
PLUGINS_ENABLED=false
PLUGINS_DIR=./plugins
PLUGINS_AUTO_LOAD=false

# ============================================
# NOTIFICATIONS
# ============================================
NOTIFICATIONS_ENABLED=true
EMAIL_NOTIFICATIONS=false
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=

# ── Internal Gateway (Unix socket — zero TCP ports) ───────────────────────────
GATEWAY_ENABLED=true
LOCAL_NODE_ENABLED=true
# Browser WebChat (loopback only — not reachable from outside)
WEBCHAT_HOST=127.0.0.1
WEBCHAT_PORT=8789
# Optional token: URL must include ?token=<value> to access the chat.
# Generate one with: python -c "import secrets; print(secrets.token_urlsafe(24))"
WEBCHAT_TOKEN=
# Set to true to also bind on the Tailscale IP (100.x.x.x) — no SSH needed
WEBCHAT_TAILSCALE=false

# ── Mobile Relay ─────────────────────────────────────────────────────────────
# Set to true when you have the Sable mobile app installed.
# Requires Tailscale (recommended) or Tor to reach from your phone.
MOBILE_RELAY_ENABLED=false
MOBILE_RELAY_HOST=127.0.0.1
MOBILE_RELAY_PORT=7891
# Leave blank to auto-generate a secret on first run (~/.opensable/mobile_secret.txt)
MOBILE_RELAY_SECRET=
# Enable if routing through a Tor hidden service
MOBILE_RELAY_TOR=false
# Enable if using Tailscale (prints Tailscale IP and QR code on startup)
MOBILE_RELAY_TAILSCALE=false
SMTP_FROM=

# ============================================
# X/TWITTER AUTONOMOUS AGENT
# ============================================
X_ENABLED=false
X_USERNAME=your_x_username
X_EMAIL=your_x_email
X_PASSWORD=your_x_password
X_AUTOPOSTER_ENABLED=false
GROK_ENABLED=false

# Content settings
X_TOPICS=geopolitics,tech,ai
X_STYLE=analyst
X_LANGUAGE=en

# Posting limits
X_POST_INTERVAL=1800
X_ENGAGE_INTERVAL=300
X_MAX_DAILY_POSTS=20
X_MAX_DAILY_ENGAGEMENTS=100

# Behavior probabilities (0.0 to 1.0)
X_REPLY_PROBABILITY=0.3
X_LIKE_PROBABILITY=0.6
X_RETWEET_PROBABILITY=0.2
X_FOLLOW_PROBABILITY=0.1
X_QUOTE_PROBABILITY=0.1
X_BOOKMARK_PROBABILITY=0.15

# Accounts to watch (comma-separated)
X_ACCOUNTS_TO_WATCH=
# Custom RSS feeds
X_CUSTOM_FEEDS=
# Dry run mode (test without posting)
X_DRY_RUN=true
X_ACTION_DELAY=3