# Trading dependencies for Open-Sable
# Install with: pip install -r requirements-trading.txt

# Exchange connectors
ccxt>=4.0.0                    # Binance, Coinbase, and 100+ exchanges
alpaca-py>=0.20.0              # Alpaca (US stocks, ETFs)

# Prediction markets
py-clob-client>=0.15.0         # Polymarket CLOB API

# DeFi / on-chain
hyperliquid-python-sdk>=0.5.0  # Hyperliquid perpetuals
solders>=0.20.0                # Solana key management (Jupiter)

# Technical analysis
ta>=0.11.0                     # Technical indicators
pandas>=2.0.0                  # DataFrames for candle data
numpy>=1.24.0                  # Numerical operations

# Async HTTP
httpx>=0.25.0                  # Async HTTP client
websockets>=12.0               # WebSocket feeds
aiohttp>=3.9.0                 # Additional async HTTP

# Database
aiosqlite>=0.19.0              # Async SQLite for portfolio DB
